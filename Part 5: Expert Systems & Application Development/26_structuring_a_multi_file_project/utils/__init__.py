# -*- coding: utf-8 -*-

# This file can be empty.
# Its presence tells the Python interpreter that the 'utils' directory
# should be treated as a "package". This allows us to use the dot-notation
# to import modules from this directory, like we do in main.py:
#
# from utils.string_helper import ...
#
# Without this __init__.py file, that import would fail with a ModuleNotFoundError.